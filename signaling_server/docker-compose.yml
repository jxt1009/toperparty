version: "3.8"

services:
  signaling:
    build: .
    container_name: signaling
    restart: unless-stopped
    ports:
      - "3000:3000"  # internal only; nginx will proxy to this
    networks:
      - web

networks:
  web:
    external: true
