version: "3.8"

services:
  signaling:
    image: node:20-alpine
    container_name: signaling
    working_dir: /app
    volumes:
      - ./server:/app
    command: ["node", "server.js"]
    ports:
      - "4001:4001"   # Expose to host
    restart: unless-stopped
