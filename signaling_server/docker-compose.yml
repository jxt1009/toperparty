version: "3.8"

services:
  signaling:
    build: .
    container_name: signaling
    restart: unless-stopped
    ports:
      - "4000:4000"  # internal only; nginx will proxy to this
    networks:
      - web

networks:
  web:
    external: true
